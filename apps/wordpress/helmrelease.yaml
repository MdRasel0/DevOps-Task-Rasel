values:
  mariadb:
    enabled: false
  externalDatabase:
    host: mysql.wordpress.svc.cluster.local
    user: wpuser
    password: wppass
    database: wordpress
  wordpressUsername: admin
  wordpressPassword: adminpass
  ingress:
    enabled: true
    hostname: app.local
